version: '3'

services:

  server:
    build: './back-end'
    ports:
      - "8080:8080"

  client_web:
    build: './front-end'
    depends_on:
      # - client_mobile
      - server
    ports:
      - "8081:8081"
    volumes:
      - apk:/mobile_app/

  # client_mobile:
    # build: './mobile-app'
    # volumes:
      # - apk:/mobile_app/

  # mongo_db:
    # image: mongo:4.4
    # ports:
      # - "27017:27017"
    # volumes:
      # - mongo-data:/data/db
    # networks:
      # - my_net

volumes:
  # mongo-data:
  apk: