version: '3'

services:
  server:
    build: ./back-end
    ports:
      - "8080:8080"
    volumes:
    


  client_mobile:
    build: ./mobile-app
    depends_on:
      - server
    volumes:


  client_web:
    build: ./front-end
    depends_on:
      - client_mobile
      - server
    ports:
      - "8081:8081"
    volumes: