version: '3'

services:

  server:
    build: './back-end'
    ports:
      - "8080:3000"

  client_web:
    build: './front-end'
    depends_on:
      - server
    ports:
      - "8081:3001"
    volumes:
      - apk:/mobile_app/

  # client_mobile:
  #   build: './mobile-app'
  #   depends_on:
  #     - server
  #   volumes:
  #     - apk:/mobile_app/

volumes:
  apk: