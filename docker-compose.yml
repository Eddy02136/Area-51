version: '3'

services:

  server:
    build: './back-end'
    ports:
      - "8080:8080"

  client_web:
    build: './front-end'
    depends_on:
      - server
    ports:
      - "8081:8081"
    volumes:
      - apk:/mobile_app/

volumes:
  apk: